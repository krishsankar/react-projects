const person = {
    name: 'Kannan',
    age: 22,
    location: {
        place: "Velachery",
        yearsofliving: 20
    }
};
const {name, age} = person;

console.log(`${name} is ${age} years old`);
